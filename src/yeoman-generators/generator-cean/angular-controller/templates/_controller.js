'use strict';

/**
 * @ngdoc function
 * @name <%= _.slugify(_.humanize(appname)) %>.controller:<%= _.slugify(_.humanize(controllername))%>
 * @description
 * # <%= _.slugify(_.humanize(controllername))%>
 */
var app = angular.module('<%= _.slugify(_.humanize(appname)) %>');

app.controller('<%= _.slugify(_.humanize(controllername))%>', function($scope) {
    
    //Your controller code here
    //...
    
});